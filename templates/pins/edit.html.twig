{% extends 'base.html.twig' %}

{% block title %} Edit {{ pin.title }} {% endblock %}

{% block body %}
    <div class="row">
        <div class="col-md-6 mx-auto">
            <h1 class="text-center">Edition de {{ pin.title }}</h1>
            {{ include ("_partials/_form.html.twig", {submitButtonText: 'Enregistrer la modification'}) }}
        </div>
    </div>
{% endblock %}